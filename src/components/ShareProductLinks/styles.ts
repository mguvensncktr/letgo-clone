import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    
    container:{
        marginTop: 25
    },
    
    innerContainer:{
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 10,
        flexWrap: 'wrap'
    }

})

export default styles